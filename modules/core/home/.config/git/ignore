# Глобальный gitignore. git читает этот путь сам, без core.excludesfile —
# поэтому в конфиге такой строки и нет.
#
# Сюда только то, что не должно попадать в ЛЮБОЙ репозиторий и чего чужому
# проекту нечего знать про мои инструменты. Всё, что специфично для проекта,
# живёт в его собственном .gitignore.

# Claude Code: локальные разрешения, машинно-специфичны и не переносимы.
**/.claude/settings.local.json
CLAUDE.local.md

# Работа: пути из рабочих репозиториев. Формально это специфика проекта, но
# рабочие проекты я завожу в разных средах, а в их .gitignore эти строки не
# протащить — поэтому держу здесь. Вне работы эти пути не встречаются.
FRONT_REVIEWER.md
gbuild
nci/tsunami/docs/architecture/
nci/.settings/org.eclipse.buildship.core.prefs
nci/.settings/org.eclipse.jdt.core.prefs
nebo-build-and-sync.sh
nebo-sync.sh
