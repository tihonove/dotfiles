# Терминал. Шрифты здесь же: fonts.conf задаёт алиас monospace на
# JetBrainsMono Nerd Font, а kitty.conf на этот алиас опирается — без него
# терминал остаётся без нердфонтовых глифов.

desc="терминал kitty + шрифты (fontconfig)"

requires="kitty"
