# Claude Code на этой машине управляет Chrome, который запущен на локальном
# компе: сокет моста приезжает сюда по ssh (скрипт claude-chrome-bridge из
# core, запускается на стороне Chrome). Вся схема и её гочи —
# ~/.system-setup/claude-chrome-remote-bridge.md.
#
# Форс нужен потому, что автовключение интеграции требует «спаренного»
# расширения на этой же машине (pairedDeviceId в конфиге) — здесь его нет
# и не будет. Пока туннель не поднят, переменная безвредна: claude просто
# не найдёт ни одного сокета.
export CLAUDE_CODE_ENABLE_CFC=true
