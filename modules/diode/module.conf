# diode — свой редактор (бывший vexx, переехал в diode-editor/diode). Payload —
# один кусок конфига git; всё остальное бинарник держит вне ~/.config: состояние
# (~/.vexx/user-data — имя внутри бинарника пока старое) и кэш бандла.
#
# requires пуст намеренно: скрипт update сам ставит бинарник, так что требовать
# его наличия до установки — замкнутый круг.

desc="diode: обновление бинарника (dogfood)"

requires=""

scripts="update"
