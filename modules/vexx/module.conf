# vexx — свой редактор. Payload'а нет вовсе: конфигов в ~ он не держит, только
# состояние (~/.vexx/user-data) и кэш бандла.
#
# requires пуст намеренно: скрипт update сам ставит бинарник, так что требовать
# его наличия до установки — замкнутый круг.

desc="vexx: обновление бинарника (dogfood)"

requires=""

scripts="update"
